# Инструкция агенту: текущая правда по КТМА/УБСИ/ЯЛК

**Перед любой работой по УБСИ сначала прочитать `PROJECT_MEMORY.md`.** Это краткая актуальная память проекта: подтверждённые ЯЛК/ЯТП протоколы, UI/UX-направление, deploy-процесс, известные запреты и ближайший маршрут до выпуска.

Перед изменениями в тракте УБСИ/ЯЛК прочитать `docs/00_КАРТА_ПРОЕКТА.md`,
`docs/05_ТЕКУЩЕЕ_СОСТОЯНИЕ.md` и `docs/08_ЖУРНАЛ_СТЕНДА.md`.

## Приоритет источников

1. ТУ/методика — источник требований, допусков и состава проверки.
2. Живой стенд и захват трафика работающей референсной KPA — источник
   фактически используемого протокола и последовательности команд.
3. Архивные исходники прошивок — полезный исторический источник, но они могут
   не совпадать с прошивкой, установленной на стенде.
4. Текущий код проекта — реализация, которую нужно сверять с пунктами выше,
   а не считать априори правильной.

Не подменять факты догадками. Если референсная KPA и архивная прошивка
расходятся, для воспроизведения текущего стенда сначала повторяется трафик KPA.

## Проверенный 29.08.2026 тракт ЯЛК

Сеть:

- ПЭВМ, Ethernet оборудования: `192.168.0.50/24`;
- SSH ПЭВМ: `192.168.0.31`, host `DESKTOP-5EO9J5A`;
- Ethernet/RS-485-адаптер: `192.168.0.115`, UDP `1113`;
- ИСД: `192.168.0.101`, HTTP/80;
- В7-78/1: `USB0::0x164E::0x0DAD::TW00053184::INSTR`.

### Адаптер: рабочий протокол референсной проверки ЯЛК

Рабочая KPA перед запуском ЯЛК отправляет на `.115:1113` четыре UDP payload
по 128 байт. Неуказанный хвост пакета заполнен нулями:

```text
reset:     52 4F 4B 54 16 00 00 00 ...
addrYALK:  52 4F 4B 54 14 01 2B 01 00 ...
addrYTP:   52 4F 4B 54 15 01 01 01 00 ...
startYALK: 52 4F 4B 54 0A 00 00 01 00 ...
```

`52 4F 4B 54` = ASCII `ROKT`.

После этого адаптер передаёт broadcast
`192.168.0.115:1113 -> 192.168.0.255:1113` с UDP payload **204 байта**:

```text
4-byte header + 100 x little-endian uint16_t
```

В подтверждённом захвате header: `02 00 2B 00`.

Для 100 слов этого формата:

```cpp
analog  = raw & 0x03FF;       // 10 bit
signal  = (raw & 0x0400) != 0;
```

Проверено нашим `orbita_ubsi_rokt_probe` на живом стенде, 16/16 кадров:

```text
ADDR 97: analog 124..125, median 125
ADDR 98: analog 920..921, median 921
ADDR 99: analog 922..923, median 922
```

Для расчёта калибровки проверки ЯЛК по текущей методике используются адреса
**97 и 99**. Наличие похожего значения на адресе 98 не делает его заменой 99.

### Важно про архивный `44 01 06`

Архивная прошивка действительно содержит `44 01 06` / `WorkMode=6` и в этом
режиме формирует быстрые 120-байтные и медленные 200-байтные кадры. Для этого
формата исторический декодер `0x01FF` / `0x0200` обоснован.

Этот факт **не отменять и не удалять**. Но это отдельный режим/ветка протокола.
Для воспроизведения текущей автоматической проверки ЯЛК референсной KPA
подтверждён именно путь `ROKT -> 204 bytes` выше. Не заменять его автоматически
на `44 01 06` только потому, что эта команда видна в архивном `easyweb.c`.

### Приём UDP на Windows

Проверенная схема сокетов:

- receive: bind `0.0.0.0:1113`, потому что адаптер передаёт broadcast;
- command sender: bind `192.168.0.50:0` (ephemeral source port);
- фильтровать принятые данные по sender `192.168.0.115:1113`.

Не возвращать receive bind на `192.168.0.50:1113`: Windows не доставлял туда
broadcast `.255:1113` в проверенной конфигурации.

## ИСД: проверенный протокол воздействия ЯЛК

Нельзя задавать точки ЯЛК как `type=1 ... val=4095`. Это была неверная
интерпретация для текущего стенда.

Захват HTTP работающей KPA показал перед воздействием:

```text
GET /type=4num=1
GET /type=7num=1
```

Точки напряжения задаются **в вольтах** командой `type=5` и с `bus=1`:

```text
GET /type=5num=1val=0.00work=1bus=1
GET /type=5num=1val=3.10work=1bus=1
GET /type=5num=1val=6.20work=1bus=1
```

Ручная проверка команды

```text
/type=5num=1val=6.20work=1bus=1
```

дала на В7-78/1 стабильные `6.145045760`, `6.145045120`, `6.144935680 V`.
Следовательно, физический путь ИСД -> линия ЯЛК -> В7 подключён и работает.

Наблюдавшееся в KPA выключение канала:

```text
GET /type=1num=1val=0work=1bus=0
GET /type=1num=1val=0work=0
```

Не придумывать отдельную `type=2` для «шина+»: захват показал, что нужная шина
для аналоговой точки задаётся параметром `bus=1` в `type=5`.

## Что сейчас не надо диагностировать заново

Уже подтверждено:

- физическая проводка тракта ЯЛК работает — референсная KPA проходит проверку;
- адаптер `.115` доступен и выдаёт рабочие кадры ЯЛК;
- WinSock/broadcast path работает;
- В7 доступен через VISA и измеряет фактическое воздействие;
- ИСД принимает `type=5` и реально выдаёт около 6.145 В на точке 6.20 В.

Не объяснять нулевой В7 «неподключёнными проводами» и не возвращаться к
сетевой диагностике без нового симптома.

## Проверенный 02.09.2026 тракт ЯТП

Свежий захват успешного запуска `KPA_Rokot` опроверг прежнюю гипотезу
`ROKT 17 01`. Текущая wire-команда запуска ЯТП — пакет 128 байт:

```text
52 4F 4B 54 0A 02 00 01 00 ...
```

После неё поток меняется с ЯЛК-204 на кадры ЯТП длиной 68 байт:
`01 00 34 00 + 32 x little-endian uint16_t`. Команды ИСД сценария KPA:
`type=7`, каналы `3/9/12/17`, включить после запуска и выключить при cleanup.
Магазин подключён к общему X123; ЯТП сама опрашивает 30 каналов.

Не возвращать `ROKT 17 01` и не считать успешные старые пробы доказательством:
они выполнялись после KPA при сохранённом режиме адаптера.

## Ближайшая инженерная цель

Сначала один канал ЯЛК end-to-end:

```text
ROKT init -> 204-byte ЯЛК -> ISD 0/3.1/6.2 V -> V7 -> 16 fresh ЯЛК samples
-> calibration 97/99 -> error -> verdict
```

Полный диагностический проход ЯЛК 80/80 уже выполнен 02.09.2026. Текущая цель —
закрепить ЯТП по захваченному `ROKT 0A 02` и проверить минимальную поставку при
постоянных 120 Ом.

Не делать широкий рефакторинг. `orbita/` — старый рабочий модуль пользователя;
без отдельного запроса его алгоритмы не переписывать.

## Текущий объём УБСИ: только ЯЛК-96 и ЯТП. Не реализовывать и не расширять проверки ЯП-П, ЯВП-8, питания, климата и прочих узлов без прямого требования пользователя.

