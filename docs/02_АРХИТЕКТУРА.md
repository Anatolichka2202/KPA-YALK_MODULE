# Архитектура стендового комплекса

## Единое приложение

Основная ветка — `master`. Единственное настольное приложение —
`OrbitaDesktop`. Отдельное окно `КТМАТестМВП` в продукт не переносится.

## Слои

- `liborbita` — приём отсчётов и проверенная реализация протокола «Орбита»;
- службы приложения — каталог каналов, допуски, карты и результаты испытаний;
- адаптеры оборудования — RS-485, VISA, АЦП, ИСД и имитаторы;
- `OrbitaDesktop` — работа оператора, графики, таблицы и итог испытания.

Декодер не должен зависеть от конкретной платы АЦП. Внутренний интерфейс
`ISampleSource` отделяет поток отсчётов от E20-10 и позволит подключать другие
драйверы, воспроизведение файла и имитатор без изменения декодера.

## Оборудование

| Устройство | Назначение | Способ подключения | Состояние |
|---|---|---|---|
| АЦП, включая E20-10 | приём аналогового сигнала «Орбита» | драйвер производителя | E20-10 работает в текущем Windows-коде; Linux-адаптер требует реализации |
| адаптер УБСИ | обмен с УБСИ | RS-485 | требуется описание кадров и команд |
| В7-78/1 | эталонное измерение напряжения | VISA | требуется реализовать плагин и проверить команды прибора |
| Я2М-66М | измерение мощности | уточняется | прибор внесён в каталог, интерфейс нужно подтвердить |
| ИСД | задание воздействий на входы ячеек | HTTP по референсной Delphi-программе | промышленный адаптер ещё не готов |

Адреса приборов, COM-порты, VISA resource и IP-адрес ИСД задаются профилем
рабочего места. В логике испытания они не прошиваются.

Каждый адаптер обязан поддерживать проверку готовности и диагностическое
сообщение. Отсутствие физического оборудования не должно мешать запуску в
режиме имитации или воспроизведения записи.
