# Вся сторонняя документация с кодом — НЕ в облако
# (содержит embedded git repos, чужие прошивки и примеры программ)
документация о системе телеметрии/ИСД/
документация о системе телеметрии/КТМА/
документация о системе телеметрии/примеры программ и настроек/
документация о системе телеметрии/Приложение 1 (аналог).docx
документация о системе телеметрии/Приложение 2 (цифра).docx
документация о системе телеметрии/протокол Орбита.pdf
# Наш анализ (анализ/) — разрешён явно (исключение из исключений)
!документация о системе телеметрии/анализ/

# Бинарные документы — конфиденциальные
**/*.pdf
**/*.docx
**/*.xlsx
**/*.db
**/*.tlm
**/*.png
**/*.ZIP
**/*.zip

# Скомпилированные артефакты
**/*.o
**/*.axf
**/*.lst
**/*.map
**/*.bin
**/*.crf
**/*.d
